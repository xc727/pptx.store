Put homepage images here.

Recommended formats:
- PNG for transparent icons and cutout graphics exported from PowerPoint.
- WebP for large hero backgrounds.
- SVG for crisp line icons and logos.

After adding images, update `assetConfig` in `../index.html`.
